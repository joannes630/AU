def main():
  week = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"]
  # Slice the list to create a new list with only the weekdays 
  # These are elements with index 1 to 5
  weekdays = week[1:6]
  print(weekdays)

main()
