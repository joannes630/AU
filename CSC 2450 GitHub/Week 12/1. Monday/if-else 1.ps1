<#
Write a script that prompts the user to enter a name. After the user provides 
the input, determine whether the name begins with the letter “J”.
If the name starts with “J”, display:
    Match
Otherwise, display:
    Does not match

if ($name -like "J*") {
Write-Output "Match"
$name = Read-Host "Enter name"
if (name -like "J*") {
} else {
}
Write-Output "Does not match"
if ($name -match "J*") {
if ($username -like "J*") {
if ($name -like "J") {

#>
