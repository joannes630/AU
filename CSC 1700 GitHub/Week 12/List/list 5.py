"""
1. Use a method of the list object to sort the elements of the list.
Print the list afterwards.

print(numbers)
numbers.sort()
sort(numbers)
numbers.sort
numbers(sort)
"""

numbers = [34, 64, 12, 1, 99]

"""
2. Use a method of the list object to insert the number 5 as the 
3rd element in the list. Print the list afterwards.

numbers = [34, 64, 1, 99]
print(numbers)
numbers.insert(5, 2)
insert(numbers(2))
numbers(insert(5, 2))
numbers.insert(2, 5)
numbers.insert(3, 5)
"""

numbers = [34, 64, 1, 99]

"""
3. Replace the 3rd element of the list with the number 5.
Print the list afterwards.

numbers.insert(2, 5)
numbers[2] = 5
print(numbers)
"""
numbers = [34, 64, 1, 99]

"""
4. Use a method of the list object to remove the 2nd element of the list.
Print the list afterwards.

numbers.remove(2)
numbers.pop(1)
numbers.pop(2)
print(numbers)
"""
numbers = [34, 64, 1, 99]

"""
5. What is the output?

list1 = [1, 2, 3]
list2 = list1
list2[1] = 101
print(list1)

6. What is the output?

list1 = [1, 2, 3]
list2 = []
for x in list1:
    list2.append(x)
list2[1] = 101
print(list1)

7. Write a function named change_all_evens(array) that takes a list of integers (array) as
a parameter and modifies the list by replacing every even number in the list with that
number plus one.


for i, num in enumerate(array):
def change_all_evens(array):
array[i] = num + 1
if num % 2 == 0:

def main():
    mylist = [1, 2, 3, 4, 5]
    change_all_evens(mylist)
    print(mylist)

main()

8. Write a function named create_5_element_list() that prompts the user to enter
five integers and stores them in a list. The function should use a loop to
collect exactly five values, converting each input to an integer before adding
it to the list. After all five numbers have been entered, return the list.

array = []
array.append(val)
return array
def create_5_element_list():
for _ in range(5):
val = int(input("Enter a number: "))

mylist = create_5_element_list()
print(mylist)


9. Using the readlines() method of a file object
with open("names.txt", "r") as file:
    array = file.readlines()

for line in array:
    print(line.strip())
"""





