"""
Write a Python function read_file(filename) that opens a file with the given filename, reads all lines one by one, removes any trailing whitespace or newline characters, and prints each line to the console.

"""
