"""
Write a Python program that opens a file named data.txt, reads the first line from the file, removes any trailing newline characters, and displays the line to the console.

Use the open() and close() functions.
"""
